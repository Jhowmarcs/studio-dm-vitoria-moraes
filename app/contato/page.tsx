import type { Metadata } from "next"; import { Location, FinalCta } from "@/src/components/Sections"; import { business } from "@/src/config/business";
export const metadata:Metadata={title:"Contato e localização",description:"Fale com o Studio DM pelo WhatsApp e encontre a localização em Itapecerica da Serra."}; export default function Page(){return <><div className="page-hero"><span>Contato</span><h1>Vamos conversar?</h1><p>Fale diretamente com Vitória para consultar serviços e horários disponíveis.</p><a className="text-link" href={`tel:${business.whatsapp}`}>{business.phone}</a></div><Location/><FinalCta/></>}
