import type { MetadataRoute } from "next"; import { services } from "@/src/data/services"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const pages=["","/servicos","/portfolio","/sobre","/avaliacoes","/contato","/politica-de-privacidade","/termos-de-uso",...services.map(s=>`/servicos/${s.slug}`)];return pages.map((p)=>({url:`${base}${p}`,lastModified:new Date(),changeFrequency:p===""?"weekly":"monthly",priority:p===""?1:.7}))}
