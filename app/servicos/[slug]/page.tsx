import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { services } from "@/src/data/services"; import { WhatsAppButton } from "@/src/components/WhatsApp"; import { JsonLd } from "@/src/components/seo/JsonLd";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const s=services.find(x=>x.slug===slug);return s?{title:s.name,description:s.description}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const msg=`Olá, Vitória! Vi o serviço de ${s.name} no site e gostaria de consultar horários disponíveis.`;return <><JsonLd data={{"@context":"https://schema.org","@type":"Service",name:s.name,description:s.description,areaServed:"Itapecerica da Serra, SP",provider:{"@type":"BeautySalon",name:"Studio DM — Vitória Moraes"}}}/><div className="page-hero"><span>{s.eyebrow}</span><h1>{s.name}</h1><p>{s.description}</p><WhatsAppButton message={msg} label={`Consultar ${s.name.toLowerCase()}`}/></div><section className="prose"><h2>Um atendimento pensado para você</h2><p>A indicação considera seus traços, sua rotina e o resultado que você deseja. Antes de começar, converse com Vitória sobre suas preferências e tire todas as dúvidas.</p><h2>Como funciona</h2><ol><li>Conversa inicial e entendimento do resultado desejado.</li><li>Avaliação individual e indicação do cuidado adequado.</li><li>Realização do serviço com orientações para o pós-atendimento.</li></ol><h2>Cuidados e manutenção</h2><p>Os cuidados e a necessidade de manutenção variam conforme a técnica indicada. As orientações específicas são informadas no atendimento, sem recomendações genéricas que possam não servir para você.</p></section></>}
