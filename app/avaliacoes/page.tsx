import type { Metadata } from "next"; import { Reviews, FinalCta } from "@/src/components/Sections"; import { getGooglePlace } from "@/src/lib/google-places";
export const metadata:Metadata={title:"Avaliações no Google",description:"Consulte avaliações verificadas do Studio DM no Google."}; export default async function Page(){const place=await getGooglePlace();return <><div className="page-hero"><span>Avaliações</span><h1>Experiências compartilhadas.</h1><p>Comentários são carregados diretamente da integração oficial com o Google, preservando autoria e atualização.</p></div><Reviews place={place}/><FinalCta/></>}
