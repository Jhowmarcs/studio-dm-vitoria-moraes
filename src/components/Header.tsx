"use client";
import Link from "next/link"; import { Menu, X } from "lucide-react"; import { useState } from "react"; import { navigation } from "@/src/data/navigation"; import { whatsappUrl } from "@/src/config/business"; import { AnalyticsLink } from "./AnalyticsLink";
export function Header(){const[open,setOpen]=useState(false);return <header className="header"><div className="nav"><Link className="brand" href="/"><span>DM</span><b>Studio DM</b></Link><nav aria-label="Principal" className={open?"menu open":"menu"}>{navigation.map(x=><Link key={x.href} href={x.href} onClick={()=>setOpen(false)}>{x.label}</Link>)}<AnalyticsLink event="click_whatsapp_header" className="button small" href={whatsappUrl()} target="_blank">Agendar</AnalyticsLink></nav><button className="menu-button" aria-label={open?"Fechar menu":"Abrir menu"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}
